---

docker_gpg_key_url: https://download.docker.com/linux/debian/gpg
docker_keyring_dir: /etc/apt/trusted.gpg.d
docker_keyring: "{{ docker_keyring_dir }}/docker.gpg"

docker_repository_url: "https://download.docker.com/linux/{{ ansible_facts.distribution | lower }}"

docker_group_user: docker
